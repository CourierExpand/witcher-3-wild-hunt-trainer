[
  {
    "path": "witcher-3-wild-hunt-trainer/requirements.txt",
    "content": "pynput==1.7.6\npsutil==5.9.5\n"
  },
  {
    "path": "witcher-3-wild-hunt-trainer/src/trainer.py",
    "content": "\"\"\"\nMain trainer module for The Witcher 3: Wild Hunt.\nProvides memory manipulation utilities for reading and writing game values.\n\"\"\"\n\nimport ctypes\nimport ctypes.wintypes\nimport psutil\nfrom typing import Optional, List\nfrom src.process_manager import ProcessManager\n\n\nclass Witcher3Trainer:\n    \"\"\"Handles memory reads/writes for The Witcher 3 game process.\"\"\"\n\n    # Common memory offsets for Witcher 3 v1.32 (GOTY) - example values\n    HEALTH_OFFSET = 0x12345678\n    MAX_HEALTH_OFFSET = 0x1234567C\n    STAMINA_OFFSET = 0x12345680\n    MONEY_OFFSET = 0x12345684\n    TOXICITY_OFFSET = 0x12345688\n\n    def __init__(self):\n        self.process_manager = ProcessManager(\"witcher3.exe\")\n        self.process_handle = None\n\n    def connect(self) -> bool:\n        \"\"\"Connect to the Witcher 3 process.\"\"\"\n        self.process_handle = self.process_manager.get_process_handle()\n        return self.process_handle is not None\n\n    def read_memory(self, address: int, size: int) -> Optional[bytes]:\n        \"\"\"Read memory from the game process.\"\"\"\n        if not self.process_handle:\n            return None\n        buffer = ctypes.create_string_buffer(size)\n        bytes_read = ctypes.c_size_t()\n        kernel32 = ctypes.windll.kernel32\n        success = kernel32.ReadProcessMemory(\n            self.process_handle,\n            ctypes.c_void_p(address),\n            buffer,\n            size,\n            ctypes.byref(bytes_read)\n        )\n        if success and bytes_read.value == size:\n            return buffer.raw\n        return None\n\n    def write_memory(self, address: int, data: bytes) -> bool:\n        \"\"\"Write memory to the game process.\"\"\"\n        if not self.process_handle:\n            return False\n        bytes_written = ctypes.c_size_t()\n        kernel32 = ctypes.windll.kernel32\n        success = kernel32.WriteProcessMemory(\n            self.process_handle,\n            ctypes.c_void_p(address),\n            data,\n            len(data),\n            ctypes.byref(bytes_written)\n        )\n        return success and bytes_written.value == len(data)\n\n    def set_health(self, value: float) -> bool:\n        \"\"\"Set Geralt's health to a specific value.\"\"\"\n        data = ctypes.c_float(value)\n        return self.write_memory(self.HEALTH_OFFSET, bytes(data))\n\n    def set_max_health(self, value: float) -> bool:\n        \"\"\"Set Geralt's max health.\"\"\"\n        data = ctypes.c_float(value)\n        return self.write_memory(self.MAX_HEALTH_OFFSET, bytes(data))\n\n    def set_stamina(self, value: float) -> bool:\n        \"\"\"Set Geralt's stamina.\"\"\"\n        data = ctypes.c_float(value)\n        return self.write_memory(self.STAMINA_OFFSET, bytes(data))\n\n    def set_money(self, value: int) -> bool:\n        \"\"\"Set Geralt's money (crowns).\"\"\"\n        data = ctypes.c_int32(value)\n        return self.write_memory(self.MONEY_OFFSET, bytes(data))\n\n    def set_toxicity(self, value: float) -> bool:\n        \"\"\"Set Geralt's toxicity level.\"\"\"\n        data = ctypes.c_float(value)\n        return self.write_memory(self.TOXICITY_OFFSET, bytes(data))\n\n    def get_health(self) -> Optional[float]:\n        \"\"\"Get current health value.\"\"\"\n        raw = self.read_memory(self.HEALTH_OFFSET, 4)\n        if raw:\n            return ctypes.c_float.from_buffer_copy(raw).value\n        return None\n\n    def get_max_health(self) -> Optional[float]:\n        \"\"\"Get max health value.\"\"\"\n        raw = self.read_memory(self.MAX_HEALTH_OFFSET, 4)\n        if raw:\n            return ctypes.c_float.from_buffer_copy(raw).value\n        return None\n\n    def get_stamina(self) -> Optional[float]:\n        \"\"\"Get current stamina.\"\"\"\n        raw = self.read_memory(self.STAMINA_OFFSET, 4)\n        if raw:\n            return ctypes.c_float.from_buffer_copy(raw).value\n        return None\n\n    def get_money(self) -> Optional[int]:\n        \"\"\"Get current money.\"\"\"\n        raw = self.read_memory(self.MONEY_OFFSET, 4)\n        if raw:\n            return ctypes.c_int32.from_buffer_copy(raw).value\n        return None\n\n    def get_toxicity(self) -> Optional[float]:\n        \"\"\"Get current toxicity.\"\"\"\n        raw = self.read_memory(self.TOXICITY_OFFSET, 4)\n        if raw:\n            return ctypes.c_float.from_buffer_copy(raw).value\n        return None\n\n    def disconnect(self):\n        \"\"\"Close the process handle.\"\"\"\n        if self.process_handle:\n            kernel32 = ctypes.windll.kernel32\n            kernel32.CloseHandle(self.process_handle)\n            self.process_handle = None\n"
  },
  {
    "path": "witcher-3-wild-hunt-trainer/src/process_manager.py",
    "content": "\"\"\"\nProcess manager for locating and attaching to The Witcher 3 process.\n\"\"\"\n\nimport psutil\nimport ctypes\nfrom typing import Optional, List\n\n\nclass ProcessManager:\n    \"\"\"Manages the game process: find, open, and close handle.\"\"\"\n\n    def __init__(self, process_name: str):\n        \"\"\"\n        Initialize with the target process name.\n        \n        Args:\n            process_name: e.g., 'witcher3.exe'\n        \"\"\"\n        self.process_name = process_name\n        self.process_handle = None\n        self.pid = None\n\n    def find_process(self) -> Optional[int]:\n        \"\"\"Find the PID of the target process.\"\"\"\n        for proc in psutil.process_iter(['pid', 'name']):\n            try:\n                if proc.info['name'] and proc.info['name'].lower() == self.process_name.lower():\n                    return proc.info['pid']\n            except (psutil.NoSuchProcess, psutil.AccessDenied):\n                continue\n        return None\n\n    def get_process_handle(self) -> Optional[int]:\n        \"\"\"Open a handle to the process with necessary permissions.\"\"\"\n        pid = self.find_process()\n        if pid is None:\n            return None\n        self.pid = pid\n        PROCESS_VM_READ = 0x0010\n        PROCESS_VM_WRITE = 0x0020\n        PROCESS_VM_OPERATION = 0x0008\n        PROCESS_QUERY_INFORMATION = 0x0400\n        desired_access = (\n            PROCESS_VM_READ |\n            PROCESS_VM_WRITE |\n            PROCESS_VM_OPERATION |\n            PROCESS_QUERY_INFORMATION\n        )\n        kernel32 = ctypes.windll.kernel32\n        handle = kernel32.OpenProcess(desired_access, False, pid)\n        if handle:\n            self.process_handle = handle\n            return handle\n        return None\n\n    def close_handle(self):\n        \"\"\"Close the process handle.\"\"\"\n        if self.process_handle:\n            kernel32 = ctypes.windll.kernel32\n            kernel32.CloseHandle(self.process_handle)\n            self.process_handle = None\n            self.pid = None\n\n    def is_process_running(self) -> bool:\n        \"\"\"Check if the target process is still running.\"\"\"\n        if self.pid:\n            try:\n                proc = psutil.Process(self.pid)\n                return proc.is_running()\n            except psutil.NoSuchProcess:\n                return False\n        return False\n"
  },
  {
    "path": "witcher-3-wild-hunt-trainer/src/hotkey_listener.py",
    "content": "\"\"\"\nHotkey listener module for triggering trainer actions.\nUses pynput to listen for global hotkeys.\n\"\"\"\n\nfrom pynput import keyboard\nfrom typing import Dict, Callable, Optional\nimport threading\n\n\nclass HotkeyListener:\n    \"\"\"Listens for global hotkeys and executes associated callbacks.\"\"\"\n\n    def __init__(self):\n        self._listener: Optional[keyboard.Listener] = None\n        self._hotkeys: Dict[str, Callable] = {}\n        self._current_keys = set()\n        self._lock = threading.Lock()\n\n    def register_hotkey(self, hotkey: str, callback: Callable):\n        \"\"\"\n        Register a hotkey combination with a callback.\n        \n        Args:\n            hotkey: e.g., 'ctrl+shift+h' for health, 'ctrl+shift+m' for money\n            callback: Function to call when hotkey is pressed\n        \"\"\"\n        keys = hotkey.lower().split('+')\n        self._hotkeys[hotkey] = callback\n\n    def _parse_key(self, key):\n        \"\"\"Convert a key event to a string representation.\"\"\"\n        try:\n            if hasattr(key, 'char') and key.char is not None:\n                return key.char.lower()\n            elif hasattr(key, 'name'):\n                return key.name.lower()\n            else:\n                return str(key).lower()\n        except:\n            return None\n\n    def _on_press(self, key):\n        \"\"\"Handle key press events.\"\"\"\n        key_str = self._parse_key(key)\n        if key_str is None:\n            return\n        with self._lock:\n            self._current_keys.add(key_str)\n            # Check registered hotkeys\n            for hotkey, callback in self._hotkeys.items():\n                required_keys = set(hotkey.lower().split('+'))\n                if required_keys.issubset(self._current_keys):\n                    # Execute in a separate thread to avoid blocking\n                    threading.Thread(target=callback, daemon=True).start()\n\n    def _on_release(self, key):\n        \"\"\"Handle key release events.\"\"\"\n        key_str = self._parse_key(key)\n        if key_str is None:\n            return\n        with self._lock:\n            self._current_keys.discard(key_str)\n\n    def start(self):\n        \"\"\"Start the hotkey listener.\"\"\"\n        if self._listener is None or not self._listener.running:\n            self._listener = keyboard.Listener(\n                on_press=self._on_press,\n                on_release=self._on_release\n            )\n            self._listener.start()\n\n    def stop(self):\n        \"\"\"Stop the hotkey listener.\"\"\"\n        if self._listener and self._listener.running:\n            self._listener.stop()\n            self._listener = None\n"
  },
  {
    "path": "witcher-3-wild-hunt-trainer/src/__init__.py",
    "content": "\"\"\"\nWitcher 3: Wild Hunt Trainer package.\n\"\"\"\n\nfrom .trainer import Witcher3Trainer\nfrom .process_manager import ProcessManager\nfrom .hotkey_listener import HotkeyListener\n\n__all__ = ['Witcher3Trainer', 'ProcessManager', 'HotkeyListener']\n"
  },
  {
    "path": "witcher-3-wild-hunt-trainer/tests/__init__.py",
    "content": ""
  },
  {
    "path": "witcher-3-wild-hunt-trainer/tests/test_trainer.py",
    "content": "\"\"\"\nUnit tests for the Witcher 3 Trainer module.\nTests are designed to work without an actual game process.\n\"\"\"\n\nimport unittest\nfrom unittest.mock import patch, MagicMock\nfrom src.trainer import Witcher3Trainer\nfrom src.process_manager import ProcessManager\n\n\nclass TestWitcher3Trainer(unittest.TestCase):\n    \"\"\"Test suite for Witcher3Trainer.\"\"\"\n\n    @patch('src.trainer.ProcessManager')\n    def setUp(self, mock_process_manager):\n        \"\"\"Set up test fixtures.\"\"\"\n        self.mock_process_manager = mock_process_manager.return_value\n        self.mock_process_manager.get_process_handle.return_value = 12345  # mock handle\n        self.trainer = Witcher3Trainer()\n        self.trainer.process_manager = self.mock_process_manager\n        self.trainer.process_handle = 12345\n\n    @patch('ctypes.windll.kernel32.ReadProcessMemory')\n    def test_read_memory_success(self, mock_read):\n        \"\"\"Test successful memory read.\"\"\"\n        mock_read.return_value = True\n        # Simulate writing to buffer by side effect\n        def side_effect(handle, addr, buf, size, bytes_read):\n            buf.value = b'\\x00\\x00\\x80\\x3f'  # 1.0 as float\n            bytes_read.contents.value = size\n            return True\n        mock_read.side_effect = side_effect\n        result = self.trainer.read_memory(0x12345678, 4)\n        self.assertIsNotNone(result)\n        self.assertEqual(len(result), 4)\n\n    @patch('ctypes.windll.kernel32.ReadProcessMemory')\n    def test_read_memory_failure(self, mock_read):\n        \"\"\"Test memory read failure.\"\"\"\n        mock_read.return_value = False\n        result = self.trainer.read_memory(0x12345678, 4)\n        self.assertIsNone(result)\n\n    @patch('ctypes.windll.kernel32.WriteProcessMemory')\n    def test_write_memory_success(self, mock_write):\n        \"\"\"Test successful memory write.\"\"\"\n        mock_write.return_value = True\n        result = self.trainer.write_memory(0x12345678, b'\\x00\\x00\\x80\\x3f')\n        self.assertTrue(result)\n\n    @patch('ctypes.windll.kernel32.WriteProcessMemory')\n    def test_write_memory_failure(self, mock_write):\n        \"\"\"Test memory write failure.\"\"\"\n        mock_write.return_value = False\n        result = self.trainer.write_memory(0x12345678, b'\\x00\\x00\\x80\\x3f')\n        self.assertFalse(result)\n\n    @patch('ctypes.windll.kernel32.ReadProcessMemory')\n    def test_get_health(self, mock_read):\n        \"\"\"Test getting health value.\"\"\"\n        def side_effect(handle, addr, buf, size, bytes_read):\n            buf.value = b'\\x00\\x00\\x80\\x3f'  # 1.0 as float\n            bytes_read.contents.value = size\n            return True\n        mock_read.side_effect = side_effect\n        health = self.trainer.get_health()\n        self.assertAlmostEqual(health, 1.0, places=5)\n\n    @patch('ctypes.windll.kernel32.WriteProcessMemory')\n    def test_set_health(self, mock_write):\n        \"\"\"Test setting health value.\"\"\"\n        mock_write.return_value = True\n        result = self.trainer.set_health(100.0)\n        self.assertTrue(result)\n\n    def test_connect_no_process(self):\n        \"\"\"Test connection when process is not found.\"\"\"\n        self.mock_process_manager.get_process_handle.return_value = None\n        trainer = Witcher3Trainer()\n        trainer.process_manager = self.mock_process_manager\n        result = trainer.connect()\n        self.assertFalse(result)\n        self.assertIsNone(trainer.process_handle)\n\n    @patch('ctypes.windll.kernel32.CloseHandle')\n    def test_disconnect(self, mock_close):\n        \"\"\"Test disconnecting from process.\"\"\"\n        self.trainer.disconnect()\n        mock_close.assert_called_once_with(12345)\n        self.assertIsNone(self.trainer.process_handle)\n\n\nclass TestProcessManager(unittest.TestCase):\n    \"\"\"Test suite for ProcessManager.\"\"\"\n\n    @patch('psutil.process_iter')\n    def test_find_process_found(self, mock_iter):\n        \"\"
